// Inserted into the page by content-idle.js as <script src=chrome.runtime.getURL('page-script.js')>:
// runs in the page's main world, as Chrome lets an extension's web-accessible script do on any
// page whatever its Content-Security-Policy says.
window.__pageScript = { ran: true, currentScript: document.currentScript ? 'script' : 'null' }
