{
  "id": "background-video",
  "order": 93,
  "class": "BackgroundVideoDemo",
  "shard": "phone-f",
  "mirrors": "android-background-video-demo.yml",
  "dir": "media-demo",
  "timeout": 1200,
  "estimate": 5,
  "note": "Background video per site (MED-08 / EDGE-32, services pass 10) on the image's own WebView 113: the loopback clip with sound under the default (block) – a plain <video> paused by the engine itself on Home, the site pausing itself on visibilitychange – then the site allowed through permissions.set: Home with the audio on for thirty seconds (page visible to itself, no visibilitychange, notification with Pause, the foreground service up), the screen locked and still playing, the return with nothing heard; the same with picture-in-picture denied; Pause from the notification while away letting the held hide through. Stills services-pass-10-media-android-*. The driver turns the lock screen on for one scene and off again at its end."
}
