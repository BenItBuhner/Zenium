{
  "id": "startup",
  "order": 98,
  "class": "StartupDemo",
  "shard": "api35",
  "mirrors": "android-startup-demo.yml",
  "script": ".github/scripts/android-startup-demo.sh",
  "env": {
    "STARTUP_ASSERT": "true",
    "STARTUP_THEMES": "light"
  },
  "setup": ["ffmpeg"],
  "timeout": 900,
  "estimate": 4,
  "note": "Its own script: the instrumentation seeds the session and walks out so the fixture tab's picture is on disk, the script performs the starts (cold with the fixture's paint held back, hot, the activity re-created) against a loopback fixture server on the runner, reads am start -W, the Fully drawn line and dumpsys window, and reads each recording frame by frame (android-startup-frames.mjs, ffmpeg). Light only here (the dispatch records both schemes); beside the status bar's boot-path driver, and the phone shards have no room for it."
}
