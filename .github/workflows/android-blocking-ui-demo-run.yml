name: Android blocking UI demo (temporary run)

# Temporary caller for one verification run of the blocking UI demo on this branch under the
# #198 driver rule: the dispatch-only android-blocking-ui-demo.yml is what stays; this file goes
# once the run has been read.
on:
  push:
    branches: [cursor/services-blocking-ui-24d1]

permissions:
  contents: read

concurrency:
  group: android-blocking-ui-demo-run-${{ github.ref }}
  cancel-in-progress: true

jobs:
  record:
    name: Blocking UI demo
    uses: ./.github/workflows/android-emulator-demo.yml
    with:
      script: .github/scripts/android-gesture-demo.sh
      script-env: |
        DEMO_CLASS=app.zen.chromium.BlockingUiDemo
        DEMO_DIR=blocking-ui-demo
        DEMO_OUT=artifacts/android-blocking-ui-demo
        DEMO_VIDEO=services-blocking-android-ui-demo.mp4
      artifact-name: android-blocking-ui-demo
      artifact-path: artifacts/android-blocking-ui-demo/

  record-again:
    name: Blocking UI demo, second boot
    needs: record
    if: ${{ failure() && needs.record.outputs.emulator-died == 'true' }}
    uses: ./.github/workflows/android-emulator-demo.yml
    with:
      script: .github/scripts/android-gesture-demo.sh
      script-env: |
        DEMO_CLASS=app.zen.chromium.BlockingUiDemo
        DEMO_DIR=blocking-ui-demo
        DEMO_OUT=artifacts/android-blocking-ui-demo
        DEMO_VIDEO=services-blocking-android-ui-demo.mp4
      artifact-name: android-blocking-ui-demo-second-boot
      artifact-path: artifacts/android-blocking-ui-demo/
